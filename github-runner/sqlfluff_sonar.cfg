[sqlfluff]
dialect = snowflake
templater = raw
max_line_length = 200

# ============================================================
# Rule ownership: SonarQube vs SQLFluff
# ============================================================
# SonarQube SQLCC:        -> SELECT *, non-sargable, cartesian joins, NULL comparisons, views
# SQLFluff (this config)  -> capitalisation, formatting, AST-based style, implicit aliases
# ============================================================

exclude_rules = AM01, CV11

[sqlfluff:templater:raw]

[sqlfluff:rules:capitalisation.keywords]
capitalisation_policy = upper

[sqlfluff:rules:capitalisation.identifiers]
capitalisation_policy = upper

[sqlfluff:rules:capitalisation.functions]
capitalisation_policy = upper

[sqlfluff:rules:capitalisation.literals]
capitalisation_policy = upper

[sqlfluff:rules:capitalisation.types]
capitalisation_policy = upper

[sqlfluff:rules:convention.select_trailing_comma]
select_clause_trailing_comma = forbid

[sqlfluff:rules:layout.select_targets]
wildcard_policy = single
