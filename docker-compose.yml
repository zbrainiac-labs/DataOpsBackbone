x-runner-base: &runner-base
  image: brainiac/local-github-runner
  restart: unless-stopped
  volumes:
    - /var/run/docker.sock:/var/run/docker.sock
    - sql_unit_report:/home/docker/sql-report-vol
    - ./sql-unit-reports:/home/docker/sql-unit-reports
  networks:
    - sonarnet

x-runner-env: &runner-env
  GITHUB_OWNER: ${GITHUB_OWNER}
  ACCESS_TOKEN: ${GH_RUNNER_TOKEN}
  SONAR_HOST: http://sonarqube:9000
  SONAR_ADMIN_PASS: ${SONAR_ADMIN_PASS}
  CONNECTION_NAME: ${CONNECTION_NAME}
  SNOW_CONFIG_B64: ${SNOW_CONFIG_B64:-}

services:
  sonarqube:
    image: brainiac/sonarqube
    container_name: sonarqube
    depends_on:
      db:
        condition: service_healthy
    ports:
      - "9000:9000"
    environment:
      SONAR_JDBC_URL: jdbc:postgresql://db:5432/${POSTGRES_DB}
      SONAR_JDBC_USERNAME: ${SONAR_JDBC_USERNAME}
      SONAR_JDBC_PASSWORD: ${SONAR_JDBC_PASSWORD}
    volumes:
      - sonarqube_data:/opt/sonarqube/data
      - sonarqube_extensions:/opt/sonarqube/extensions
    healthcheck:
      test: ["CMD-SHELL", "curl -sf http://localhost:9000/api/system/status | grep -q '\"status\":\"UP\"'"]
      interval: 30s
      timeout: 10s
      retries: 10
      start_period: 120s
    networks:
      - sonarnet

  db:
    image: postgres:17.5
    container_name: sonarqube_db
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB}
    volumes:
      - postgresql:/var/lib/postgresql/data
      - ./backup:/docker-entrypoint-initdb.d
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER} -d ${POSTGRES_DB}"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 30s
    networks:
      - sonarnet

  runner1:
    <<: *runner-base
    container_name: github-runner-${GITHUB_REPO_1}
    depends_on:
      sonarqube:
        condition: service_healthy
    environment:
      <<: *runner-env
      REPO_URL: https://github.com/${GITHUB_OWNER}/${GITHUB_REPO_1}
      RUNNER_NAME: runner-${GITHUB_REPO_1}
      RUNNER_WORKDIR: /_work/runner-${GITHUB_REPO_1}
      GITHUB_REPO: ${GITHUB_REPO_1}
      LABELS: runner-${GITHUB_REPO_1}
      PROJECT_KEY: ${GITHUB_REPO_1}
      DISABLE_AUTOMATIC_DEREGISTRATION: "true"
      CONFIGURED_ACTIONS_RUNNER_FILES_DIR: /actions-runner-config/${GITHUB_REPO_1}
      UNSET_CONFIG_VARS: "true"
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - runner_config:/actions-runner-config/${GITHUB_REPO_1}
      - sql_unit_report:/home/docker/sql-report-vol
      - ./sql-unit-reports:/home/docker/sql-unit-reports

  runner2:
    <<: *runner-base
    container_name: github-runner-${GITHUB_REPO_2}
    depends_on:
      sonarqube:
        condition: service_healthy
    environment:
      <<: *runner-env
      REPO_URL: https://github.com/${GITHUB_OWNER}/${GITHUB_REPO_2}
      RUNNER_NAME: runner-${GITHUB_REPO_2}
      RUNNER_WORKDIR: /_work/runner-${GITHUB_REPO_2}
      GITHUB_REPO: ${GITHUB_REPO_2}
      LABELS: runner-${GITHUB_REPO_2}
      PROJECT_KEY: ${GITHUB_REPO_2}
      DISABLE_AUTOMATIC_DEREGISTRATION: "true"
      CONFIGURED_ACTIONS_RUNNER_FILES_DIR: /actions-runner-config/${GITHUB_REPO_2}
      UNSET_CONFIG_VARS: "true"
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - runner_config:/actions-runner-config/${GITHUB_REPO_2}
      - sql_unit_report:/home/docker/sql-report-vol
      - ./sql-unit-reports:/home/docker/sql-unit-reports

  nginx-server:
    image: nginx
    container_name: nginx-server
    ports:
      - "8080:80"
    volumes:
      - ./nginx/default.conf:/etc/nginx/conf.d/default.conf
      - sql_unit_report:/usr/share/nginx/html:ro
    healthcheck:
      test: ["CMD-SHELL", "curl -sf http://localhost:80/ || exit 1"]
      interval: 15s
      timeout: 5s
      retries: 3
      start_period: 10s
    networks:
      - sonarnet

volumes:
  sonarqube_data:
  sonarqube_extensions:
  postgresql:
  runner_config:
  sql_unit_report:
    name: sql_unit_report

networks:
  sonarnet:
